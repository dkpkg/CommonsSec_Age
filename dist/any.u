# Distribution for CommonsSec_Age

## age + plugins

  $ run-rule CommonsSec_Age.Age.Files@1.3.1 -d ${RUNTIME}/age slot=${SLOTNAME.Release.target_abi}
  \test(pass)
  <!-- The `\dk.object(abi: "...", value-id: "...")` lines are generated by dk0 on
       the first build (one per supported ABI: Release.Windows_x86_64,
       Release.Darwin_x86_64, Release.Darwin_arm64). Run the local get-bundle /
       run-rule validation in AUTHORING.md, then let dk0 record them here. -->
